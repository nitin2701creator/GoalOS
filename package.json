{
  "name": "goalos",
  "version": "0.0.1",
  "private": true,
  "description": "GoalOS — AI Operating System for Business Automation (documentation repo)",
  "type": "module",
  "scripts": {
    "dev": "sh -c '.venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "start": "sh -c '.venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "build": "sh -c 'rm -rf dist && mkdir -p dist && cp -r agents app development docs workflows readme.md dist/'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
