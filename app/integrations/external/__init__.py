"""External capability adapters for GoalOS.

Each adapter wraps an external service behind a clean GoalOS-level interface.
Business logic never imports external project code directly — only through
these adapter modules.
"""
