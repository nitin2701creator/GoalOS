# GoalOS runtime dependencies (KVM Docker image).
# Aider, Codex, or VS Code are NOT required — GoalOS is fully autonomous
# on the KVM using these packages plus the Python standard library.
fastapi==0.141.1
uvicorn==0.52.1
sqlalchemy==2.0.51
pydantic==2.13.4
pypdf==6.15.0
requests==2.34.2
google-genai>=1.0.0
cryptography>=42.0.0
PyJWT>=2.8.0
psutil>=5.9.0
playwright==1.45.0
httpx==0.27.0 # Required by fastapi.testclient
typing_extensions
# trio is a library for async concurrency, typically not a top-level requirement for GoalOS, but can be added if specific async needs arise
# asyncio is part of standard library, no need to specify version
