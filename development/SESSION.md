# Autonomous Development System Session

## Session Context

This file is reserved for concise, human-readable handoff notes during ADS work.

## Current Handoff

- **Full suite green: 480 passed, 0 failed** (`.venv/bin/python -m pytest`; +25 tests). New integration-layer code ruff-clean (only the repo-wide `Depends` convention remains).
- **Twenty CRM + social capability layer + webhook foundation built** (no parallel architecture, no fake providers, no Aider):
  - **Twenty connector** (`app/integrations/twenty.py`): `TwentyConnector` over the shared HTTP client with `Authorization: Bearer <key>` against a fully configurable base URL (`GOALOS_TWENTY_BASE_URL`, cloud or self-hosted) + `/rest/` endpoints — no custom-object assumptions. 13 capabilities (search/create/update for people, companies, opportunities; create/update task; create note; get_record by object slug + id) with `READ_CRM`/`WRITE_CRM` permissions (writes are in `DANGEROUS_PERMISSIONS`). Distinct error handling: 401/403 → `AuthenticationError` (`AUTHENTICATION_FAILED:...`), 429 → `RateLimitError` (`RATE_LIMITED:...`), other 4xx/5xx and malformed JSON → structured `ConnectorError`; both transport paths (returned 4xx and raised `HttpStatusError`) are mapped. Missing `GOALOS_TWENTY_BASE_URL`/`GOALOS_TWENTY_API_KEY` → honest `Not Configured`/`INTEGRATION_NOT_CONFIGURED`; keys never logged.
  - **Social capability layer** (`app/integrations/social.py`): `SocialConnector` registers the full surface — `social.meta.*` (create_post/publish_post/get_post/get_insights), `social.linkedin.*`, `social.x.*`, `social.reddit.*` (create/publish/get/get_analytics) = 16 capabilities — but implements NO provider calls and reads NO credentials; every capability honestly reports unavailable (`INTEGRATION_NOT_CONFIGURED`) until a real provider connector is wired. Publish/create require `PUBLISH_SOCIAL` (dangerous); reads require `READ_SOCIAL`.
  - **Approval control**: `CapabilityDefinition.requires_approval` (new field, persisted on `capabilities.requires_approval` — new column, dev DB migrated) marks every Twenty write and every social create/publish. The execution runtime persists a NEW `APPROVAL_REQUIRED` blocked state whenever such a capability runs outside an approved workflow (`workflow.requirement` set — the existing approval marker); inside an approved workflow it runs normally. The runtime therefore distinguishes allowed / `PERMISSION_DENIED` / `APPROVAL_REQUIRED` / `INTEGRATION_NOT_CONFIGURED` / `EXECUTION_FAILED`.
  - **Capability registry**: 29 new definitions seeded idempotently (`_integration_definition` with provider `twenty`/`social`), connectors registered in `build_default_registry` (`SUPPORTED_INTEGRATIONS` + `twenty`, `social`), exported from `app/integrations/__init__.py`.
  - **Webhook/event foundation**: `EventRecord` model (`events` table, registered in `app/db/models/__init__.py`), `EventRepository`, `WebhookService` (`app/services/webhook_service.py`), `POST /api/v1/webhooks/twenty` + `GET /api/v1/webhooks/events` (`app/api/v1/webhooks.py`, registered in `app/api/router.py`). Signature validation per Twenty's current docs: HMAC-SHA256 hex of `{X-Twenty-Webhook-Timestamp}:{raw body}` with `GOALOS_TWENTY_WEBHOOK_SECRET`, constant-time compare (legacy `X-Twenty-Signature` header accepted too). Replay protection: signed timestamp within a 5-minute tolerance + deterministic event key (event type + record id + event timestamp) de-duplicated in the DB (duplicate delivery → 200 idempotent). Valid events persist as `received` before the 202; invalid signatures/timestamps reject with 401 and persist as `rejected` for audit; no secret configured → 503. A `register_handler` dispatch seam exists so events can later be routed into the existing workflow/scheduler/execution architecture.
  - **Deployment config**: `app/config.py` documents `GOALOS_TWENTY_BASE_URL`/`GOALOS_TWENTY_API_KEY`/`GOALOS_TWENTY_WEBHOOK_SECRET` (+ `TwentySettings.from_env`) and the social placeholder vars (`GOALOS_META_PAGE_ACCESS_TOKEN`, `GOALOS_LINKEDIN_ACCESS_TOKEN`, `GOALOS_X_ACCESS_TOKEN`, `GOALOS_REDDIT_CLIENT_ID/SECRET/USERNAME/PASSWORD` — not read yet); `docker-compose.yml` gained the Twenty vars + commented social placeholders.
  - **Tests (+25)**: `tests/test_twenty_connector.py` (12: not-configured honesty, search/create/update over mocked HTTP, distinct auth/rate-limit/malformed errors, permission denial, runtime execution persistence, APPROVAL_REQUIRED outside an approved workflow, write allowed inside an approved workflow), `tests/test_social_capabilities.py` (6: all 16 registered, publish requires PUBLISH_SOCIAL + approval, honest Not Configured resolve/execute, workflow never claims fake success), `tests/api/test_webhooks_api.py` (7: valid 202 + persisted, invalid signature 401 + rejected audit record, tampered body, stale timestamp replay rejection, duplicate idempotency, missing headers, missing secret 503).
  - **Live acceptance (real API + real goalos.db)**: 13 twenty + 16 social capabilities seeded; `/api/v1/integrations` shows `twenty` and `social` as `Not Configured`; `twenty_search_people` execute → `INTEGRATION_NOT_CONFIGURED`; `social_meta_publish_post` runtime → `failed`/`INTEGRATION_NOT_CONFIGURED`; webhook ingest on the real DB: valid event → `received` + persisted, replay → `duplicate`, bad signature → rejected + persisted (`signature_valid False`) for audit.
  - **KVM configuration (not coded)**: `GOALOS_TWENTY_BASE_URL` + `GOALOS_TWENTY_API_KEY` for real CRM data (create the API key in Twenty under Settings → API & Webhooks; rate limit 100 req/min); `GOALOS_TWENTY_WEBHOOK_SECRET` matching the secret entered when creating the Twenty webhook (target `https://{goalos}/api/v1/webhooks/twenty`); social provider vars remain unconfigured by design until provider connectors are implemented. Existing deployments need one ALTER: `ALTER TABLE capabilities ADD COLUMN requires_approval BOOLEAN DEFAULT 0` (fresh DBs get it automatically via `Base.metadata.create_all`; the `events` table is created automatically).
- **Full suite green: 455 passed, 0 failed** (`.venv/bin/python -m pytest`; +26 tests). New scheduler/runtime/workflow-control code ruff-clean (only the repo-wide `Depends` convention remains).
- **Core runtime complete — scheduler wired into the execution runtime + deployment readiness** (no parallel architecture, no new business integrations, no Aider):
  - **Persisted scheduler wired into the execution runtime** (`app/services/scheduler_service.py` + extended `app/integrations/scheduler.py`): schedules persist on the existing `Workflow` model and survive restart; each due run executes through the SAME canonical path as manual execution (`ExecutionRuntimeService.run_workflow`) as a NEW run instance cloned from the template (`workflows.scheduled_from_id` — new column), so every run keeps its own persisted state, start/end times, success/failure, and execution history while the template keeps its cadence. Duplicate prevention at three levels: atomic DB claim on the template (`SchedulerConnector.claim` compare-and-set on `next_run_at`, `synchronize_session=False`), in-flight run-instance guard (`has_in_flight_run` — a crash mid-run is never re-cloned), and the runtime's own per-workflow in-flight guard. `run_due` advances the template in `finally` (a crash never wedges the schedule); disabled/cancelled schedules are never due; `scheduler.create` requires explicit `SCHEDULE_WORKFLOWS` (never bypassed).
  - **Canonical lifecycle completed** (`app/db/models/runtime_execution.py`): `pending → running → succeeded | failed | blocked | cancelled`. Workflow pre-flight blockers persist as `blocked` executions with the precise reason + code (never a fake success); cancelling a workflow marks its in-flight executions `cancelled` (`error_code CANCELLED`).
  - **Stable failure codes** (new `RuntimeErrorCode` in `app/services/execution_runtime.py`, persisted on the new `runtime_executions.error_code` column): `INTEGRATION_NOT_CONFIGURED`, `PERMISSION_DENIED`, `CAPABILITY_NOT_FOUND`, `WORKFLOW_INVALID` (workflow ValueErrors are prefixed with it), `EXECUTION_FAILED`, plus `DISABLED`/`CANCELLED`. Granted permissions are persisted in `execution_metadata.granted_permissions` so retries restore the exact grant — never escalate.
  - **Autonomous workflow control**: `WorkflowService.pause/resume/cancel` (`Cancelled` added to `WorkflowStatus`) + API `POST /workflows/{id}/pause|resume|cancel` (cancel also cancels in-flight runtime executions). `ExecutionRuntimeService.retry(execution_id)` (fresh execution from a failed/blocked/cancelled one, `retried_from` metadata) + `POST /executions/runtime/{id}/retry`; `ExecutionRuntimeService.retry_workflow(workflow_id)` (fresh run instance cloned from a failed workflow, history retained) + `POST /workflows/{id}/retry`. Execution history: `GET /executions/runtime?workflow_id=&status=&capability=` filters + `stats()`.
  - **Schedule API** (`app/api/v1/schedules.py`, registered in `app/api/router.py`): `GET/POST /api/v1/schedules` (create/update; create requires `SCHEDULE_WORKFLOWS`), `POST /{id}/enable|disable`, `DELETE /{id}` (cancel), `POST /run-due` (operator tick), `POST /{id}/run-now` (manual trigger through the runtime).
  - **Startup/worker behavior** (`app/control_loop/scheduler_worker.py` + `app/config.py`): process-wide singleton loop started in `app.main` startup and stopped on shutdown — restarts and multiple app instances never stack loops; multiple uvicorn workers are safe via the DB claim. Config centralized in `RuntimeSettings.from_env()`: `GOALOS_SCHEDULER_ENABLED` (default 1), `GOALOS_SCHEDULER_INTERVAL` (30s), `GOALOS_SCHEDULER_CLAIM_HORIZON` (900s). No hard-coded credentials/providers/URLs.
  - **Deployment readiness**: `GET /ready` (200 only when the DB answers), `GET /health` + `GET /v1/health` now report app, database, LLM provider configuration, the scheduler worker (running/enabled/interval/last tick/tick count — honest), persisted execution stats, and integration config state. `docker-compose.yml` gained scheduler env vars + a `/ready` healthcheck.
  - **DB/persistence**: models registered in `app/db/models/__init__.py`; dev `goalos.db` migrated (ALTER ADD COLUMN `runtime_executions.error_code`, `workflows.scheduled_from_id`). SQLite tz handling: naive datetime reads coerced to UTC (`_coerce_utc`) so schedule comparisons are safe.
  - **Tests (+26)**: `tests/test_scheduler_service.py` (16: permission-gated create, lifecycle disable/enable/cancel, due-run through the real pipelines with persisted executions + template advance + restart durability, honest blocked integration, in-flight skip, claim duplicate prevention, run-now, workflow cancel → cancelled executions, retry execution (same permissions, `retried_from`), retry-workflow clone + history, stable error codes, list filters/stats, worker duplicate-loop refusal) and `tests/api/test_schedules_api.py` (10: permission denial, schedule CRUD, run-due end to end, blocked integration honesty, run-now, pause/resume/cancel, cancel cancels in-flight executions, workflow retry, execution filters/retry, /ready + /health with worker + executions).
  - **Live acceptance (real API + real goalos.db)**: `/ready` 200 database healthy; `/health` shows worker `scheduler running` + execution stats; schedule created (daily) → run-due → run instance `Failed` honestly with `keyword_research` step `blocked`/`INTEGRATION_NOT_CONFIGURED` (web.search unconfigured) and `scheduled_from_id` linked; pause→Paused (schedule disabled), resume→Pending (schedule re-enabled), cancel→Cancelled; calculation executes `{result: 42.0}`; retry of a succeeded execution refused with `EXECUTION_FAILED: only failed, blocked, or cancelled executions...`.
  - **Remaining gaps (honest, unchanged)**: no new business integrations — gmail/woocommerce/GA4/meta/whatsapp/ocr/video/memory and `web.search` without `GOALOS_SEARCH_PROVIDER` still report `INTEGRATION_NOT_CONFIGURED`. On the KVM, set `GOALOS_SEARCH_PROVIDER=duckduckgo` (and the integration credentials) for real data; `GOALOS_SCHEDULER_ENABLED=1` gives autonomous due-run execution with the app.
- **Full suite green: 429 passed, 0 failed** (`.venv/bin/python -m pytest`; +21 tests). New execution-runtime code ruff-clean (only the repo-wide `Depends` convention remains).
- **Execution Runtime implemented** — a persisted capability-execution layer on top of the existing capability engine, permission model, connector registry, agent factory, and workflow persistence (no parallel architecture, no new external integrations, no Aider):
  - **Model** (`app/db/models/runtime_execution.py`, table `runtime_executions`, registered in `app/db/models/__init__.py`): one row per capability execution with `workflow_id` (nullable FK), `capability`, status enum (`pending`/`running`/`succeeded`/`failed`), `input`/`output` JSON, `error`, `provider`, `permissions_required`, `agent_name`, `execution_metadata` (resolution details), `started_at`/`completed_at`/`created_at`/`updated_at`. Repository: `app/repositories/runtime_execution_repository.py` (create/get/list/list_by_workflow/list_by_capability/update/delete/`active_for_workflow`). Schemas: `app/schemas/runtime_execution.py` (`RuntimeExecuteRequest`, `RuntimeExecutionResponse`, `RuntimeWorkflowRunRequest`, `RuntimeWorkflowRunResponse`).
  - **Runtime service** (`app/services/execution_runtime.py`): clean `CapabilityExecutor` interface (protocol) + `CapabilityServiceExecutor` (delegates to the existing `CapabilityService.execute`) + `ExecutionRuntimeService`. `execute(capability, params, permissions, workflow_id=..., agent_name=..., metadata=...)` runs the full lifecycle: resolve through the capability registry → permission gate (missing permissions persist as failed, never escalated) → availability gate (unconfigured provider persists as failed with the honest INTEGRATION_NOT_CONFIGURED reason, never dispatched) → `pending` → `running` → `succeeded`/`failed` with output/error/timestamps/metadata. Duplicate prevention: an in-flight (`pending`/`running`) execution for a workflow blocks a second submission; a workflow with persisted steps cannot be run again. `run_workflow(workflow_id, ...)` accepts an APPROVED workflow (via the new `WorkflowService.approve`), resolves the agent through the existing factory for permissions when none are given (or uses explicit permissions), pre-flights each capability (unregistered/unconfigured/unauthorized steps are persisted as `Blocked` with precise reasons and a failed runtime execution each — the workflow never claims success), executes each step through the runtime (real connectors/skills), and persists steps/results/evaluation in the same shape as the agent workflow path.
  - **Workflow integration**: `WorkflowService.approve(workflow_id, requirement, capabilities=..., resolved_capabilities=..., capability_service=...)` persists the approved plan (requirement + resolved capabilities) without executing. New API: `POST /api/v1/workflows/{id}/approve` and `POST /api/v1/workflows/{id}/run-runtime` (returns workflow + per-step runtime executions); `POST /api/v1/executions/runtime` (execute one capability), `GET /api/v1/executions/runtime` (list, optional `?workflow_id=`), `GET /api/v1/executions/runtime/{id}`. The chat service's run-workflow intent now approves through `WorkflowService.approve` and runs through `ExecutionRuntimeService.run_workflow` (same deterministic result shape, so the OpenWebUI tests are unchanged).
  - **Tests (+21)**: `tests/test_execution_runtime.py` (13: full lifecycle + persisted output/timestamps/metadata, permission denial, honest INTEGRATION_NOT_CONFIGURED, unregistered capability, restart durability across sessions, requires-approval, requires-permissions-or-agent-factory, approved workflow completing through the real web.search/website.crawl pipelines with persisted executions + duplicate refusal, blocked-integration honesty with `web.search` named, direct-permission run, nonexistent workflow, in-flight duplicate refusal) and `tests/api/test_execution_runtime_api.py` (8: execute/list/get via API, permission denial, INTEGRATION_NOT_CONFIGURED, 404, approve→run-runtime end to end with 2 succeeded executions + duplicate refusal, blocked integration with persisted failed execution, backward compatibility with the classic task-bound `/api/v1/executions` endpoints).
  - **Live acceptance (real API + real goalos.db)**: POST /api/v1/executions/runtime calculation {a:40,b:2} with EXECUTE_CODE → `succeeded {result: 42.0}` with started/completed timestamps (denied without EXECUTE_CODE); web_search with READ_WEBSITE → `failed` `INTEGRATION_NOT_CONFIGURED: no search provider configured`; goal→project→workflow → approve (5 capabilities resolved) → run-runtime → workflow `Failed` honestly, `keyword_research` step `Blocked` with the `web.search` reason, 1 persisted failed runtime execution, evaluation persisted; runtime records queryable via API and directly in the DB.
  - **Remaining gaps (unchanged, honest)**: no new integrations were added; gmail/woocommerce/GA4/meta/whatsapp/ocr/video/memory providers and `web.search` without `GOALOS_SEARCH_PROVIDER` still report INTEGRATION_NOT_CONFIGURED. The runtime is ready to be the single execution path for the persisted scheduler loop when that is wired.
- **Full suite green: 408 passed, 0 failed** (`.venv/bin/python -m pytest`; +28 tests). New capability-engine code ruff-clean (only the repo-wide `Depends` convention remains).
- **Capability engine implemented** — GoalOS now has a persistent, authoritative capability registry + resolver + executor, wired into the EXISTING agent factory, workflow orchestrator, connector registry, permission model, and persistence (no parallel systems, no Aider):
  - **Persistent registry**: `capabilities` table (`Capability` model + `CapabilityRepository`, registered in `app/db/models/__init__.py`) with id/name/description/category/version/status/required_permissions/input+output schemas/provider_type/provider/implementation/execution_capability/keywords/enabled/timestamps. Seeded idempotently from `app/agents/capability_definitions.py` (`BUILTIN_CAPABILITIES`, 28 rows): the 10 existing catalog capabilities (skill-backed), 11 integration-backed capabilities (`web_search`, `web_fetch`, `website_crawl`, `website_analyze`, `seo_audit`, `google_analytics_read`, `woocommerce_read`, `gmail_read`, `gmail_send`, `meta_ads_read`, `meta_ads_create`), and 7 placeholders (`whatsapp_send`, `document_extract`, `ocr`, `voice_transcription`, `video_generation`, `memory_store`, `memory_retrieve`) that are REGISTERED but report honest INTEGRATION_NOT_CONFIGURED until a provider/implementation is wired. New permissions `SEND_WHATSAPP`/`GENERATE_MEDIA` (dangerous) + `ACCESS_MEMORY`.
  - **Resolver/executor** (`app/services/capability_service.py`): `register` (idempotent), `get`/`get_by_name`/`list`/`list_with_status`, `enable`/`disable`, `resolve(name, permissions)` / `resolve_many` (exists/enabled/available/reason/required_permissions/permissions_sufficient/missing), `match(requirement)` (deterministic keyword match over registry keywords, then LLM refinement via the existing provider — only registered names are ever accepted), `resolve_for_goal` (matched registry names + catalog-ordered deduped execution capabilities for agent reuse/creation), `check_available` (integration connectors / skill implementations / placeholders; honest INTEGRATION_NOT_CONFIGURED reasons), `check_permissions` (never escalates), `get_provider`, and `execute(name, params, permissions)` — dispatches integration capabilities to the existing connectors (`web.search`, `website.analyze`, ...) and skill/native capabilities to the existing skill implementations, returning structured `OK` / `INTEGRATION_NOT_CONFIGURED` / `PERMISSION_DENIED` / `DISABLED` / `NOT_FOUND` / `ERROR` — never a fabricated success.
  - **Workflow integration**: `WorkflowService.run_agent_workflow` accepts `capability_service` + `resolved_capabilities`; the run-agent endpoint builds the engine (with the configured LLM provider) so goals resolve through the persistent registry, and the matched capability names are persisted on the new `workflows.resolved_capabilities` column (dev DB migrated). The chat service (`ChatService`) now resolves both intents through the engine (create-agent + run-workflow) and persists the resolved capabilities on the workflow.
  - **API** (`app/api/v1/capabilities.py`, registered in the router): `GET /api/v1/capabilities` (list with availability), `POST /api/v1/capabilities` (idempotent register), `GET/POST /api/v1/capabilities/{name}` (resolve), `POST /resolve-many`, `POST /match` (goal → capabilities + execution set), `POST /{name}/execute` (explicit `permissions` required — never implicit), `POST /{name}/enable|disable`.
  - **Tests (+28)**: `tests/test_capability_service.py` (18: register/retrieve, idempotent duplicate, seed, resolve existing/missing/many, goal match incl. seo_audit/website_crawl, execution-capability mapping, whatsapp_send/ocr honest NOT_CONFIGURED, PERMISSION_DENIED without EXECUTE_CODE, calculation → 42, dangerous-write gate, disable/enable, search-provider-dependent availability, real web_search via connector with fake transport, LLM refinement accepting only registered names, restart durability) and `tests/api/test_capabilities_api.py` (10: list with availability, idempotent register, resolve/resolve-many/match endpoints, calculation acceptance (resolve → execute → 42, denied without EXECUTE_CODE), unconfigured web_search → INTEGRATION_NOT_CONFIGURED, workflow through the engine with real crawl+search sources + resolved_capabilities persisted + agent reuse (subset) + agent creation (new requirement), chat path invoking the engine with resolved capabilities persisted).
  - **Live acceptance (real API + real goalos.db)**: 28 capabilities listed with honest availability (calculation/seo_audit/website_crawl available; web_search/gmail_send/whatsapp_send/video_generation/memory_store INTEGRATION_NOT_CONFIGURED); resolve calculation; execute {a:40,b:2} with explicit EXECUTE_CODE → `42.0` (denied without); web_search without provider → honest INTEGRATION_NOT_CONFIGURED; match "Analyse Organigram's website SEO at https://www.organigram.com." → [keyword_research, seo_audit, web_fetch, website_analysis, website_crawl] → execution [keyword_research, website_analysis]; workflow Completed with real web.search + website.crawl sources and resolved_capabilities persisted; chat path returns Completed with the engine resolution; fresh engine over goalos.db keeps all 28 capabilities and the workflow's resolved_capabilities intact.
  - **Remaining unconfigured (honest)**: gmail (search/read/draft/send), woo commerce reads, GA4, meta ads reads, whatsapp, OCR/transcription/video/memory providers, and `web.search` without `GOALOS_SEARCH_PROVIDER` — each reports INTEGRATION_NOT_CONFIGURED rather than faking.
- **Full suite green: 380 passed, 0 failed** (`.venv/bin/python -m pytest`; +19 tests). New OpenWebUI/chat code ruff-clean (only the repo-wide `Depends` convention remains, same as every existing endpoint).
- **Production OpenWebUI interface implemented** — GoalOS now exposes an OpenAI-compatible surface for OpenWebUI on the KVM, driving the EXISTING agent factory + workflow orchestrator (no second system, no Aider):
  - **Endpoints** (`app/api/v1/openai.py`, mounted at `/v1` in `app/main.py`): `GET /v1/models` (returns `goalos-autonomous` in OpenAI models-list format), `POST /v1/chat/completions` (accepts `model`/`messages`/`temperature`/`stream`/`tools`/`tool_choice`; returns OpenAI-format JSON; `stream` emits a single-chunk SSE stream ending `data: [DONE]`), `GET /v1/health` (structured GoalOS/database/LLM/integrations status, no secrets). `GET /health` upgraded to the same payload. Every `/v1/*` request requires `Authorization: Bearer $GOALOS_OPENWEBUI_API_KEY` via constant-time compare (503 when the key env is missing, 401 on bad key; keys never logged).
  - **ChatService** (`app/services/chat_service.py` + `app/schemas/chat.py`): deterministic intent detection (`create_agent` markers vs `run_workflow`); agent creation through `AgentFactoryService` (reuse existing ACTIVE agent or create; capabilities/skills/integrations resolved; dangerous permissions — EXECUTE_CODE/SEND_EMAIL/WRITE_WEBSITE/MODIFY_ADS/SCHEDULE_WORKFLOWS — are NEVER auto-authorized: the request is refused with an explicit message pointing at `POST /api/v1/agents`); workflow runs through the existing `WorkflowService.run_agent_workflow` with explicit capability sets (`AgentFactory.resolve_for_capabilities` + `run_agent_workflow(capabilities=...)` so resolution cannot drift from intent); conversation context is carried into the persisted workflow `requirement` (no separate conversation store); honest `INTEGRATION_NOT_CONFIGURED` prefix when a required integration is unavailable; final response is a deterministic summary of the persisted workflow, polished by the configured LLM provider only when credentials exist (`llm_configured` gates on the API key, never the FreeLLM placeholder health flag).
  - **LLM config** (`app/ai/config.py`): production aliases `LLM_API_BASE_URL`/`LLM_API_KEY`/`LLM_MODEL` added ahead of the legacy `FREELLM_*` names; `LLM_PROVIDER=openai|openai_compatible` via the existing `ProviderFactory` -> `OpenAICompatibleProvider`.
  - **New capability**: `sales_analysis` (woocommerce + google_analytics integrations, read-only permissions) with `SalesAnalysisSkill` — "Analyze Organigram sales" now resolves to real WooCommerce/GA4 pipelines; skill/agent/workflow blocker code now maps capability prefixes to registry names via `integration_for_capability` (`email`→gmail, `analytics`→google_analytics, `meta`→meta_ads) — fixes a latent GA4/gmail/meta blocker bug.
  - **Deployment** (KVM, no Aider): `requirements.txt`, `Dockerfile` (python:3.12-slim, uvicorn on 0.0.0.0:8000, `GOALOS_DATABASE_URL` volume), `docker-compose.yml` (goalos + optional freellmapi service on the Docker network by service name, `OPENWEBUI_BASE_URL` for CORS, all integration/LLM env vars); `app/db/session.py` reads `GOALOS_DATABASE_URL`.
  - **Tests (+19)**: `tests/api/test_openai_api.py` (16: models format, auth 401/503, chat completions shape, agent creation through the factory with resolved capabilities/skills/integrations, workflow run with the REAL crawl+search pipelines hermetically + persisted goal→project→workflow, honest INTEGRATION_NOT_CONFIGURED, dangerous-action refusals with no artifacts, SSE streaming, conversation-context preservation, LLM-provider polish when configured, structured health without secrets, WooCommerce+GA4 sales run with fake transport + token provider, no-Aider-in-process) and `tests/test_chat_service.py` (5 unit: intent markers, last-user-message/requirement context, LLM gating, dangerous refusal, sales agent resolution).
  - **Live acceptance (real API + real goalos.db)**: `GET /v1/models` 200; "Create an agent capable of analyzing Organigram website SEO." -> `chat.completion` with the ACTIVE agent (reused); SEO run WITHOUT search provider -> `INTEGRATION_NOT_CONFIGURED ... web.search ...`; with provider -> Completed, 2 steps, keyword_research from `web.search`, website_analysis from `website.crawl` (4 pages, real findings); streaming 3 chunks + `[DONE]`; `/v1/health` shows integrations (web/website/scheduler Healthy, gmail/woocommerce/google_analytics/meta_ads Not Configured) and `llm: not_configured`; restart keeps every run persisted. Also backfilled legacy NULL `schedule_enabled` rows in the dev DB + hardened `WorkflowResponse` with a `None → False` coercion so pre-migration rows validate.
  - **Env vars for OpenWebUI/KVM**: `GOALOS_OPENWEBUI_API_KEY` (required), `OPENWEBUI_BASE_URL` (CORS), `LLM_PROVIDER`/`LLM_API_BASE_URL`/`LLM_API_KEY`/`LLM_MODEL` (gateway), plus the existing integration vars. `.env.example` stays blocked by the sandbox guard — names above are the canonical list.
  - Next work: run the persisted scheduler loop, swap deterministic skills for connector-backed ones, and add a chat-completions payload option to `OpenAICompatibleProvider` for native LLM chat.
- **Full suite green: 361 passed, 0 failed** (`.venv/bin/python -m pytest`; +26 tests). New integration code ruff-clean (only the repo-wide `Depends`/pre-existing `email/config.py` findings remain).
- **Real integration layer implemented** — GoalOS integrations now run over real HTTP with no fabricated success, wired into the existing AgentFactory/Workflow/permission architecture:
  - **Framework**: `app/integrations/http_client.py` (shared stdlib `urllib` transport: timeout, redirects, status/content-type handling, max response size, query params, injectable opener); `app/integrations/integration_connector.py` (`IntegrationConnector` extends `BaseConnector`: capability names in `system.action` convention, per-capability `CAPABILITY_PERMISSIONS`, configuration health with new `Not Configured` status, `execute(capability, params, permissions)` dispatch with `PermissionDeniedError`); `app/integrations/factory.py` (`build_default_registry(session)` composition root from env + `SUPPORTED_INTEGRATIONS`); `GET /api/v1/integrations` exposes connector/capability/status health (no secrets).
  - **Connectors**: `web` (real fetch + `web.search` behind a swappable `SearchProvider`; `DuckDuckGoSearchProvider` parses real DDG results; no provider -> `web.search` unavailable, never faked), `website` (same-domain SEO crawler: BFS with page/depth limits, title/meta-description/H1/canonical/robots/status/word-count/links, robots.txt info, technical findings incl. missing title/meta, multiple H1, noindex, thin content, HTTP), `woocommerce` (REST v3 products/orders/customers/inventory + stock update gated by WRITE_WEBSITE), `google_analytics` (GA4 Data API runReport/runRealtimeReport with dimensions/metrics/date ranges/filters, normalized rows; service-account token provider, injectable), `meta_ads` (Graph API ad accounts/campaigns/adsets/ads/insights reads; write capabilities declared with MODIFY_ADS but NOT enabled — explicit error), `scheduler` (DB-persisted scheduled workflows on the existing Workflow model: create/list/cancel/due/advance, survives restart; `scheduler.create` requires the new `SCHEDULE_WORKFLOWS` permission), `gmail` (existing stack extended: `create_draft` + `GmailRESTService` — real REST over shared HTTP with refresh-token OAuth, no SDK; `email.search/read/draft/send` capabilities, `email.send`/`email.draft` gated by SEND_EMAIL).
  - **Skill/agent wiring**: `SkillDefinition.required_integrations`, `CapabilitySpec.integrations` + `integration_capabilities`, `AgentDefinition.integrations`, `Agent`/`Skill` DB columns + schemas; integration-aware skill implementations (website_analysis crawls through the website connector, keyword_research/web_research/company_discovery search) with deterministic fallback only when no registry is supplied; `DynamicAgent` carries the registry into skill execution; `AgentFactory.integration_blockers()` + `execute_agent(..., integrations=)` enforce availability+permissions before execution; `WorkflowService.run_agent_workflow(..., integration_registry=)` marks steps `Blocked` with persisted reasons and fails the workflow honestly when a required integration is unavailable.
  - **Tests (+26)**: `tests/test_integration_connectors.py` (18: HTTP timeout/status/max-size/params, web fetch+search parsing, crawler same-domain/limits/findings/robots, WooCommerce reads+write permission, GA4 NOT_CONFIGURED/AUTH_REQUIRED/report normalization, Meta reads+write-blocked, scheduler permission+restart) and `tests/test_integration_skills.py` (7: declarations, definition collection, persistence, real-connector skill execution, deterministic fallback, blockers, honest execution failure); API workflow e2e updated to run the REAL crawl/search pipelines hermetically (fake `urlopen` serving fixtures) + new `test_workflow_fails_honestly_when_integration_unconfigured`.
  - **Live acceptance (real API + real goalos.db)**: integration discovery shows gmail/woocommerce/google_analytics/meta_ads `Not Configured` and web/website/scheduler healthy; Organigram goal -> workflow run -> capabilities resolved -> agent created -> 4/4 steps Completed with real crawl (4 pages, findings, score 75) and real search results (provider duckduckgo) -> evaluation Passed -> second run WITHOUT search provider fails honestly with `web.search` blocker persisted -> scheduler persists a daily run -> restart keeps workflow COMPLETED, schedule enabled, steps/evaluation intact.
  - **Env vars** (no secrets hard-coded; `.env.example` is blocked by the sandbox guard — names below): `GOALOS_SEARCH_PROVIDER` (duckduckgo), `GOALOS_GMAIL_CLIENT_ID/SECRET/REFRESH_TOKEN`, `GOALOS_WOO_URL/CONSUMER_KEY/CONSUMER_SECRET`, `GOALOS_GA4_PROPERTY_ID/CLIENT_EMAIL/PRIVATE_KEY`, `GOALOS_META_ACCESS_TOKEN/AD_ACCOUNT_ID`.
- Next work: run the persisted scheduler in a real loop (execute due runs through the workflow orchestrator), and replace the deterministic content/lead skills with connector-backed implementations now that the framework is in place.
- **First real Organigram autonomous workflow implemented** — a business goal now flows Goal -> capability analysis -> agent/skill creation -> execution -> persisted results/evaluation through the EXISTING orchestrator layers (no new parallel system):
  - Extended the existing `Workflow` model (requirement/steps/results/evaluation/error_message columns) + `WorkflowResponse` schema; dev `goalos.db` `workflows` table migrated with ALTER ADD COLUMN (was empty, non-destructive).
  - `WorkflowService.run_agent_workflow(workflow_id, requirement, agent_factory)` — the orchestration method: resolves the requirement into capabilities via `resolve_capabilities` (capability catalog), reuses an existing ACTIVE agent or creates the missing one through `AgentFactoryService` (dangerous permissions still require explicit authorization — an unprivileged run fails honestly), executes through the existing `BaseAgent` runtime, persists every step status/result, the aggregate results, and a deterministic evaluation on the workflow record, then transitions PENDING -> RUNNING -> COMPLETED/FAILED with progress. Duplicate runs rejected (`already been run`); unresolvable requirements and missing agents persist a FAILED state with `error_message`.
  - Deterministic input derivation (`derive_workflow_input`): subject = last capitalized proper noun (acronyms excluded), URL/region regex/keyword extraction — general rules, no example special-casing.
  - API: `POST /api/v1/workflows/{workflow_id}/run-agent` `{requirement: str}` -> updated `WorkflowResponse` with steps/results/evaluation (404 missing workflow, 400 duplicate run).
  - Tests (+10): `tests/test_agent_workflow.py` (6: full Organigram run with 4 completed capability steps + persisted artifacts, agent/skill reuse across runs, duplicate rejection, unresolvable -> Failed, dangerous-capability authorization guard -> Failed, restart durability) and `tests/api/test_agent_workflow.py` (4: goal -> project -> workflow -> run-agent e2e with persisted steps/results/evaluation + created agent discoverable via GET /agents + resolve finds it, duplicate 400, unresolvable Failed, missing workflow 404).
  - **Live acceptance (real API + real goalos.db)**: Organigram goal created -> workflow run resolved 4 capabilities (keyword_research, website_analysis, content_analysis, web_research) -> "Keyword Research Agent" created with its skills -> all 4 steps Completed with real results (keywords ['organigram best practices', ...], website findings for https://www.organigram.com score 72, word_count, research findings) -> evaluation Passed, progress 100 -> second goal/workflow reused the existing agent (agents count unchanged) -> resolve finds the existing agent -> fresh engine over goalos.db shows the COMPLETED workflow with steps/results/evaluation intact.
- Next work: real external tool implementations behind the deterministic skills (web search, email) so workflow results reflect live data, and control-loop routing of planned work to factory-created agents.
- **Agent Factory implemented** — GoalOS now creates specialized agents from a business requirement, registers them with the existing runtime registry, and executes them through the existing `BaseAgent` contract (plan/execute/report with `AgentContext`/`AgentResult`; no second execution engine):
  - **Domain primitives**: `app/agents/permissions.py` (`Permission` + `DANGEROUS_PERMISSIONS` + action mapping), `app/agents/capabilities.py` (capability catalog + deterministic keyword→capability resolution, general not example-special-cased), `app/agents/agent_definitions.py` (`AgentDefinition` frozen Pydantic contract + `build_agent_definition`), `app/skills/definitions.py` (reusable `SkillDefinition` + `BUILTIN_SKILLS` catalog).
  - **Runtime**: `app/agents/factory/skill_implementations.py` (deterministic `BaseSkill` implementations keyed by name; one definition reused across any number of agents) and `app/agents/factory/dynamic_agent.py` (`DynamicAgent` + `build_dynamic_agent_class`; placed in a subpackage so `AgentLoader`'s exact-set discovery assertion is untouched).
  - **Persistence**: `Agent`/`Skill` models (agents, skills tables) + `AgentRepository`/`SkillRepository`, registered in `app/db/models/__init__.py`; schemas `app/schemas/agent.py` + `app/schemas/skill.py`.
  - **Service**: `app/services/agent_factory.py` — `create_agent` (capability resolution → structured definition → permission enforcement → skill ensure → persist → validate → register), `validate_agent` (DRAFT → VALIDATING → ACTIVE/FAILED with reasons; ACTIVE only after validation succeeds), `register_agent`, `update_agent` (re-validates; preserves DISABLED), `enable_agent`/`disable_agent`, `get_agent`/`get_agent_by_name`/`list_agents`, `resolve(requirement)` (existing ACTIVE agent covering the capabilities, else a deterministic spec), `execute_agent` (self-heals the runtime instance from persisted state so execution survives restarts). Dangerous permissions (EXECUTE_CODE, SEND_EMAIL, WRITE_WEBSITE, MODIFY_ADS) are never granted implicitly.
  - **API**: `app/api/v1/agents.py` — `POST/GET /api/v1/agents`, `GET/PATCH /api/v1/agents/{id}`, `POST /api/v1/agents/{id}/enable|disable|execute`, `POST /api/v1/agents/resolve`; registered in `app/api/router.py`.
  - **Tests (+22)**: `tests/test_agent_factory.py` (17 service tests: create, validate fail/ACTIVE-only-after-validation, registry discovery, skill reuse, missing-skill creation, duplicate skill/agent prevention, permission enforcement, disable/enable, resolve→existing agent + resolve→spec, execution through the BaseAgent contract, restart durability, end-to-end sum-of-two-numbers, update preserves DISABLED, get/list) and `tests/api/test_agents.py` (5 API tests against an isolated SQLite DB incl. resolve→create→execute returning 42.0 and permission 422).
  - **Live acceptance (real API + real DB)**: resolve("Create an agent capable of calculating the sum of two numbers.") → spec → create with explicit `EXECUTE_CODE` → ACTIVE → execute {"a":40,"b":2} → `42.0`, errors=[] → resolve again finds the existing agent → disable blocks execute (400) → enable restores → permission-less create rejected (422) → fresh engine over the same DB still shows the ACTIVE agent, skill, permissions, and input schema intact.
- **Coding executors implemented** (`app/kernel/development/executors.py`): a `CodingExecutor` abstraction replaces the mock/CLI worker as the production implementation path of the autonomous loop.
  - `NativeGoalOSCodingExecutor` (production, no external CLI): inspects the repository, reads in-scope files into a prompt, asks the configured GoalOS LLM provider (`app/llm` `BaseProvider`/`ProviderFactory`) for a JSON edit plan (`{"files": [{path, content}], "delete": [...]}`), parses it (`EditPlanParser`, tolerates code fences), validates every path with `SafeFileEditor` (rejects `..`/absolute paths outside the workspace, `.env*`, `.ssh`, `credentials`, `secrets`, `id_rsa`/`id_ed25519`, `*.pem/*.key/*.p12/*.pfx/*.p8`), applies the changes to disk, and returns the real `modified_files` in the existing `WorkerResult` contract. Deletes are supported so a file an earlier attempt created out of scope can be removed. No fake success: unparseable provider output, unsafe paths, or empty plans fail the run with a persisted reason; an unhealthy provider makes `available()` False and the loop fails cleanly.
  - `AiderCodingExecutor` (OPTIONAL dev adapter): wraps the existing `AiderWorker`; available only when `aider` is on PATH. Production never depends on it — the loop prefers the executor and the acceptance test runs with Aider absent.
  - `create_coding_executor(name, repository)` factory (`native`/`aider`, ValueError otherwise).
- **LLM provider**: `app/llm/openai_compatible_provider.py` adds `OpenAICompatibleProvider` on the EXISTING infrastructure (same `LLMConfig` env vars, same `FreeLLMClient` HTTP client, same `LLMGateway` text extraction; no new key mechanism). Registered in `ProviderFactory` via `LLM_PROVIDER=openai|openai_compatible`. `health_check()` requires base URL + model + key.
- **Wiring**: `AutonomousLoop(executor=...)` prefers the executor in IMPLEMENTING (worker stays as fallback, all prior tests unchanged); repair feedback (test failures/review findings) is passed to the executor and included in its prompt via the same `PromptBuilder`; `ExecutionService.run_autonomous(executor=...)`; `TaskExecuteRequest.executor`; `POST /api/v1/tasks/{task_id}/autonomous-execute` accepts `"executor": "native" | "aider"` (invalid name → 400).
- **Tests (+27)**: `tests/kernel/development/test_executors.py` (18: parser, safe-editor workspace/credential rejection, native executor real edits/prompt context/feedback/garbage-response failure/unsafe rejection/unavailable provider, Aider availability + delegation through the existing worker, factory); `tests/kernel/development/test_autonomous_executor.py` (6: full native lifecycle without Aider with real commit, repair iteration, review-finding fix via plan deletion, retry limit + no commit, unavailable provider, Aider-available full loop); service + API tests for native persistence and honest failure. `DeterministicEditProvider` (test helper) returns real edit plans through the `BaseProvider` contract.
- **Live acceptance**: native executor through the real API with `Aider on PATH: None` — inspect → provider plan → real file change → real `python -m pytest` → review/verifier pass → real commit → COMPLETED, tree clean, provider consulted once, state/commit intact after reopening the DB.
- Next work: `DevelopmentMemory` persistence and a real chat-completions payload for `OpenAICompatibleProvider` (currently uses the shared client's `{model, prompt}` shape).

## Prior Session Notes

- **Full suite green: 276 passed, 0 failed** (`.venv/bin/python -m pytest`).
- **Autonomous loop implemented** (`app/kernel/development/autonomous.py`): `AutonomousLoop` drives an objective through a persisted state machine — `PLANNING -> IMPLEMENTING -> TESTING -> (FIXING -> IMPLEMENTING -> TESTING)* -> REVIEWING -> (FIXING -> IMPLEMENTING -> TESTING -> REVIEWING)* -> COMMITTING -> COMPLETED` (or `FAILED`). It composes the existing ADS boundaries: `RepositoryInspector` (keyword-scoped file targeting over `RepositoryReader`), kernel `DevelopmentTask`/`PromptBuilder`, a `DevelopmentWorker`, `DevelopmentTestRunner` (real `python -m pytest` subprocess), `DevelopmentVerifier` + `DevelopmentReviewer`, and `GitManager.commit`. Every state transition is reported through `on_state` so callers persist the run; `max_attempts` hard-bounds the repair cycle; the repository is committed ONLY after verification + review pass, so failures never commit and preserve committed state. Repair prompts include previous test/review feedback.
- **Reviewer implemented** (`app/kernel/development/reviewer.py`): deterministic `DevelopmentReviewer.review(task, result, test_run) -> ReviewResult` — worker success/output, at least one changed file, in-scope changes only, test suite passed. Findings are fixable and bounded by the loop's attempt limit.
- **Test runner** (`app/kernel/development/test_runner.py`): `DevelopmentTestRunner.run(command, cwd) -> TestRun`. Resolves `python` to `sys.executable` and redirects bytecode to a throwaway `PYTHONPYCACHEPREFIX` per run — this fixed a real staleness bug where same-second, same-size edits left a stale `__pycache__` that masked a fix.
- **GitManager.commit(message)** added as the single mutating operation (stages `git add -A`, commits, returns HEAD hash), called exclusively by the loop after verification. Pushing/history rewrites remain out of scope.
- **Persistence**: `Execution` gained `state`, `attempts`, `test_results`, `errors`, `review_results`, `commit_hash` columns (schemas updated). `ExecutionService.run_autonomous()` claims the execution, runs the loop with an `on_state` callback that persists every transition, then finalizes with verification verdict, result, commit hash, and `Task.status`/`result`. New endpoint `POST /api/v1/tasks/{task_id}/autonomous-execute` (same duplicate-prevention + atomic claim as `run_task`).
- **Autonomous loop tests**: `tests/kernel/development/test_autonomous.py` (9 tests, real temp git repos + real pytest subprocesses + real commits): successful implementation + commit, test-fix iteration, reviewer-finding fix (out-of-scope file), failed state, retry limit, no commit on failure, exact state-transition order, API-layer scoping, blank-objective rejection. `tests/test_autonomous_lifecycle.py` (6): full persisted state/artifacts/commit on success, failed state + artifacts, restart durability, duplicate rejection, per-transition persistence. `tests/api/test_autonomous_execution.py` (2): bounded failure through the real API and duplicate rejection. Shared deterministic worker: `tests/kernel/development/helpers.py` (`ScriptedWorker`).
- **Live verification**: autonomous loop through the real API with a fake `codex` CLI (delegating non-codex subprocesses to the real runner) in a temp git repo — execution COMPLETED with 1 attempt, real `python -m pytest` passed, review passed, real commit (`ADS: Make calculator.add return the sum of its arguments.`), working tree clean, and state/commit intact after reopening the DB.
- Next work: `DevelopmentMemory` persistence and an LLM-driven planner that replaces the deterministic phase template while keeping the same `DevelopmentTask` contract.

## Prior Session Notes

- **Full suite green: 259 passed, 0 failed** (`.venv/bin/python -m pytest`). All four previously failing tests (`app/agents`/`app/skills` discovery, `app/kie` PO extraction) are fixed.
- **Persistent execution lifecycle implemented** on the existing DB architecture (routers -> services -> repositories): `Execution` gained `verification_status`/`verification_summary` columns; `ExecutionRepository` gained atomic `claim()` (Pending -> Running with rowcount guard) and `active_execution()`; `ExecutionService.run_task()` runs submit -> claim -> execute (kernel worker) -> verify -> persist result/verification/final status, updates `Task.status`/`result`, and rejects duplicates (active execution or double claim). New endpoint `POST /api/v1/tasks/{task_id}/execute` with optional `worker_type` (mock/codex/aider/claude/openhands).
- Fixed a latent tz bug in `complete_execution`/`fail_execution` (SQLite naive `started_at` vs aware `now`) via `ExecutionService._duration_seconds`.
- Loader fixes: `AgentLoader`/`SkillLoader` no longer crash on PEP 585 aliases (`SkillClass: TypeAlias = type[BaseSkill]` passes `inspect.isclass` and is a `types.GenericAlias` with `__module__ == "builtins"`); module-definition check now runs before `issubclass`. PO extractor `po_number` no longer spans lines (removed `\s` from the capture class).
- Tests: `tests/test_task_execution_lifecycle.py` (claim atomicity, duplicate prevention, failure/verification/unavailable-worker persistence, restart durability) and `tests/api/test_execution_lifecycle.py` (full API lifecycle against an isolated SQLite DB: goal -> project -> task -> execute -> verify -> persist -> duplicate 400 -> restart).
- Live verification: end-to-end run through the real API + DB showed task Completed, verification Passed, result persisted, duplicate submission rejected, and state intact after reopening the database.
- ADS execution pipeline implemented: `DevelopmentPlanner` (deterministic objective decomposition), `DevelopmentScheduler` (dependency-aware eligibility), `DevelopmentVerifier` (success/output/file-scope checks), and `DevelopmentOrchestrator` (approval -> plan -> schedule -> execute -> verify with an audit trail).
- `DevelopmentService` exposes `run_objective` / `preview_objective` end to end; the existing `add_task` / `execute_next` behavior is unchanged.
- API endpoints: `POST /api/v1/development/execute` and `POST /api/v1/development/preview`.
- **Real workers implemented**: `CLIWorker` subprocess base plus `CodexWorker` / `AiderWorker` / `ClaudeWorker` / `OpenHandsWorker`, resolved through `WorkerRegistry` (availability via `shutil.which`, `create_worker` factory, `WorkerUnavailableError`). Workers run inside the repository with a timeout, capture stdout/stderr, and detect changed files via the read-only `GitManager` (`git status --porcelain` delta).
- `GitManager` boundary implemented (read-only status/change detection; no mutations by design).
- Orchestrator gained `worker_registry` + per-task worker dispatch honoring `task.worker`; a task whose CLI is missing is BLOCKED with the reason in the run summary (no silent mocks). `run(objective, worker_type=...)` and `service.run_objective(objective, worker_type=...)` override the planner's worker type.
- API `DevelopmentRunRequest` gained optional `worker_type`; `_service_for(request)` builds a registry-based service rooted at `GOALOS_REPOSITORY` (default cwd). Omitted `worker_type` keeps the in-memory mock path (API/tests unchanged).
- Public kernel interfaces exported from `app/kernel/development/__init__.py`.
- `app/compat.py` provides a `StrEnum` backport so the project also imports on Python 3.10 (stdlib 3.11+).
- Tests: 86 kernel/ADS + API tests pass (planner, scheduler, verifier, orchestrator, service, worker/CLI workers/registry, git_manager, API routes). Full suite: 242 passed, 4 pre-existing failures.
- Verified end to end: a fake `codex` CLI ran through the real subprocess machinery in a temp Git repo — all 4 tasks completed and the worker-created file was detected in `modified_files`.
- Next work: implement `DevelopmentReviewer` (code-quality/safety review after verification), `DevelopmentMemory` persistence, and an LLM-driven planner that replaces the deterministic phase template while keeping the same `DevelopmentTask` contract.
